# TODO — prioritized queue for the next agents

Each item is self-contained: context doc, definition of done, verification. Work top-down
unless the user redirects. P0 = user-facing/user-requested, P1 = ship-blocking for a demo,
P2 = structural, P3 = polish/opportunistic.

> **This file is the ENGINEERING-CONTINUITY queue (finish what's in flight + audit blockers).**
> For the full **complete-a-polished-game** scope the user asked for — audio, music, VFX,
> animation, UI design, cinematics, adversary variety, storyline — see
> **`PRODUCTION_ROADMAP.md`** (tracks B–F). For the evidence-cited blocker/loophole list across
> Tingen + Yumina, see **`PRODUCTION_GAPS.md`**. Highest-impact single item across all of it:
> **wire combat VFX (roadmap C1 / GAP-2.10) — combat is currently visually invisible.**
>
> ⚠️ Repo is PUBLIC (`github.com/MaEnqiMark/Tingen-Game`), no LICENSE, 44 commits unpushed. Do
> not push without user approval (keys are safe — `.env` untracked).

## P0.1 — Finish the city-fill rework (USER-FLAGGED, half-done)
Read `DESIGN_fill_rework.md`. Diagnosis verdicts exist (or regenerate via WORKFLOWS.md §2);
run the apply-loop (retrim pads from keyed/orig / world-px tweaks / regens for wrong-content)
→ re-render → verify judges → ≤4 rounds. DONE = every block severity ≤1 against the painting,
whole-map critic clean, suite green incl. placements-legal, committed with per-block table.
The USER looks at the composite — that's the acceptance bar.

## P0.2 — Verify/land the interiors wiring commit
Read `DESIGN_interiors_wiring.md` §verify-first. If uncommitted, finish doors + suite + commit.
Then play-test every portal round-trip (TESTING_WORKFLOW.md §4 step 8). DONE = 10 door asserts
green at HEAD, portals walkable both ways in a real play session.

## P0.3 — Close the overnight loop
Task #29 (M8 review loop) is the last open combat task: write the closing RUNLOG morning-report
entry (combat arc summary + spend ledger + the two open MORNING DECISIONS for the user:
(a) University occupies the Almshouse's painted lot; (b) Chapel sits ~258px north of its painted
compound — both need a user call: move the building or amend the map). Tag the repo
(`git tag combat-arc-v1`). Mark #29/#32/#33 done as they land.

## P1.1 — Full-suite green gate at HEAD
After P0.1/P0.2: `--import` + all four layers (TESTING_WORKFLOW.md §0–2) + one live gate
(live_butcher, 11/11) + a §4 interactive playtest pass with screenshots. Fix anything red.
This is the "demo-able" checkpoint.

## P1.2 — Production blockers from the audit
Read `PRODUCTION_GAPS.md` §2 (critical blockers) and execute in its §6 attack order. That file
was generated by an evidence-cited 8-auditor sweep at handoff; every finding has file:line
evidence. Re-verify evidence before fixing (tree moved since).

## P1.3 — Reload/ammo pickups + HUD empty feedback
Read `DESIGN_ammo_items_followups.md` §1. DONE = rounds can be replenished in-world, empty-gun
refusal has player feedback, TDD asserts, review round.

## P2.1 — NPC cost enforcement + lootable weapons
`DESIGN_ammo_items_followups.md` §2 (recommendation: literal carried items + generic
AgentCostProvider + loot on downed bodies). Design-first: write the design section, get user
sign-off (it changes the RPG/hidden-monster texture), then M-loop it.

## P2.2 — Kit-from-carried-weapons derivation
`DESIGN_ammo_items_followups.md` §4. Touches COMBAT_SPEC §1.2/§2 + vector regeneration —
follow the spec governance strictly.

## P2.3 — Yumina combat port
COMBAT_SPEC.md §8 checklist + §8.1 persistence table + the 95 vectors are the whole contract;
Yumina's `packages/engine/src/combat/` keep/replace/add list is explicit. Check
`PRODUCTION_GAPS.md` for the Yumina-side audit first (esp. whether the godot-pivot decision
makes the TS port dual-track or moot — that's a USER decision if unresolved).

## P2.4 — Doomsday pacing decision (design gap)
`PASSIVE_DOOMSDAY_TICK` is disabled; a full playthrough's pacing is driven only by cult task
progress. Someone must decide the intended session length and re-derive pacing (see audit).
USER decision; prepare options.

## P3.1 — Animations/VFX wiring audit
`assets/anim/` strips (cultist_robed, wraith_shadow, bieber) + `assets/fx/` flipbooks landed in
M7; verify what is actually PLAYED by NPC.gd/CombatExecutor `_play_anim` vs sitting unused;
wire the gaps. Screenshot-verify in a live fight.

## P3.2 — Audio (nothing exists), main menu/settings, onboarding
Expect these in the audit's design-gaps. Scope with the user before building — demo vs product.

## Standing follow-ups (small, do opportunistically)
- `Perception` combat block: surface "you just transformed" more loudly (post-transform LLM
  idled 2 beats in the live acceptance — in-spec but observable; RUNLOG 04:4x).
- COMBAT_SPEC review noted the fired_counts/§8.1 table should gain a vector when the Yumina
  port starts (currently engine-tested only).
- `docs/handoff/*` and `CLAUDE.md` baselines: update the suite numbers when they move.
