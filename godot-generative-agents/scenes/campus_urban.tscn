[gd_scene load_steps=2 format=3]

[ext_resource type="Script" path="res://scripts/tiled_map.gd" id="1_tiled"]

[node name="CampusUrban" type="Node2D"]

; NOTE: do not scale this TileMapLayer — scaling a tilemap leaves sub-pixel gaps
; between cells (the old white seams). Size the view with the camera zoom instead.
[node name="Map" type="TileMapLayer" parent="."]
script = ExtResource("1_tiled")
map_path = "res://maps/upenn_core_urban.tmj"

; The map is the 34th–36th x Spruce–Walnut core (5632x7040 px at 1 m/tile).
; Centred on the map; zoom 0.176 fits the whole (tall) area to the 1000x1250
; portrait window (see project.godot). Size the view with the camera zoom, never by
; scaling the TileMapLayer (that leaves sub-pixel white seams between cells).
[node name="Camera2D" type="Camera2D" parent="."]
position = Vector2(2810, 3510)
zoom = Vector2(0.176, 0.176)
